# Результаты автоматизированного тестирования

Дата контрольного запуска: 27.06.2026.

Команды запуска из папки `backend`:

```powershell
python -m coverage erase
python -m coverage run manage.py test users pizzeria
python -m coverage report -m
```

Результат:

- выполнено 11 backend-тестов;
- все тесты завершились успешно;
- покрытие прикладного кода приложений `users` и `pizzeria` составляет 86%;
- из расчета исключены тесты, миграции, Django Admin, management commands и служебные файлы.

Тесты проверяют регистрацию и JWT-аутентификацию, профиль, публичное чтение каталога, административный CRUD пицц, поиск и фильтрацию, серверный расчет заказа, валидацию корзины, разграничение доступа к заказам, избранное, отзывы и запуск сценария статусов после условной оплаты.
